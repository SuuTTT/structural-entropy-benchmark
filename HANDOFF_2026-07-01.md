# HANDOFF — Public SE benchmark
**2026-07-01** · repo: github.com/SuuTTT/structural-entropy-benchmark (PUBLIC) · Pages: suuttt.github.io/structural-entropy-benchmark

## What this is
The PUBLIC companion benchmark released with the TGINA survey: per-run artifacts + code behind every
empirical claim ("does SE actually help?"), plus a GitHub Pages companion site (MathJax inline in posts).
This is the "compare it" link shared with the community (alongside selib for "use it").

## STATUS: stable, public. Referenced from the survey's Data Availability statement.

## PENDING / GOTCHAS
- Keep in sync with the private reappraisal suite (structural-entropy-survey-clean/benchmark/). This
  public repo is the released subset — do not leak unpublished private results into it.
- Pages site source = main /docs. New writeups as Jekyll posts (not bare .md), per owner's blog convention.
- Anti-fabrication: only publish numbers backed by artifacts in the repo.
